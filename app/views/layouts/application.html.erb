<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Thundrs" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
  <%= render "layouts/header" %>
  <p><h3>Connecting through past experience.</h3></p>
  <div class="row">
    <div class="large-12 columns">

      <% flash.each do |key, value| %>
        <div class"alert-box round <%= flash_case(key) %>">
          <%= value %>
        </div>
      <% end %>

      <div class="row">
        <%= yield %>
      </div>
      <%= render "layouts/footer" %>
      <%= javascript_include_tag "application" %>
    </div>
  </div>
  </body>
</html>

<%= form_for([@memory,@comment]) do |f| %>
  <h2>Add Comment</h2>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit "Save", id: "comment_save", class: "button" %>
  </div>
<% end %>
